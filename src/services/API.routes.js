export const endpoint = "url.api";

export const ROUTES = {
    
    //Pagos 
    //No uso Delete sino Update para persistir la info en BD 
    CREATEPAY: `${endpoint}/pay`,
    READPAYS: `${endpoint}/pays`,
}